<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dialogViewmodels="clr-namespace:HipMobileUI.Viewmodels.DialogViewmodels;assembly=HipMobileUI"
             x:Class="HipMobileUI.Pages.CaptionReferencePage">
  <ContentPage.BindingContext>
    <dialogViewmodels:CaptionReferenceViewmodel/>
  </ContentPage.BindingContext>
  <ContentPage.Content>
    <ListView x:Name="SourcesDemoList" ItemsSource="{Binding References}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Padding="15,0" Orientation="Vertical">
              <Label Text="{Binding SubstituteText}"/>
              <Label Text="{Binding Text}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ContentPage.Content>
</ContentPage>