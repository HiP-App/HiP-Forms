<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mainScreenContainables="clr-namespace:HipMobileUI.Viewmodels.MainScreenContainables;assembly=HipMobileUI"
             x:Class="HipMobileUI.Views.MainScreenViews.StressTestView">
  <ContentView.BindingContext><mainScreenContainables:StressTestViewModel /></ContentView.BindingContext>
    <ContentView.Content>
        <StackLayout>
            <Entry Keyboard="Numeric" Text="{Binding NumberofPages}" />
            <Button Command="{Binding OpenManyPages}" Text="Open pages!" />
            <Entry Keyboard="Numeric" Text="{Binding NumberOfImages}" />
            <Button Command="{Binding OpenPageWithManyImages}" Text="Open page with images!" />
        </StackLayout>
    </ContentView.Content>
</ContentView>
