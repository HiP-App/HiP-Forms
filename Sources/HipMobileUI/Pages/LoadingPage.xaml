<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:HipMobileUI.Controls;assembly=HipMobileUI"
             x:Class="HipMobileUI.Pages.LoadingPage"
             BackgroundColor="White">
    <ContentPage.Content>
        <StackLayout x:Name="OuterStack" Orientation="Vertical" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Spacing="20">
            <StackLayout Orientation="Vertical" Spacing="20" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                <Label Text="{Binding Text}" HorizontalTextAlignment="Center" FontSize="20" FontAttributes="Bold" IsVisible="{Binding IsExtendedViewsVisible}"/>
                <Label Text="{Binding Subtext}" HorizontalTextAlignment="Center" FontSize="16" IsVisible="{Binding IsExtendedViewsVisible}"/>
                <ActivityIndicator IsRunning="True" Color="{StaticResource AccentColor}"/>
                <Label Text="{Binding LoadingProgressText}" HorizontalTextAlignment="Center" FontSize="16" IsVisible="{Binding IsExtendedViewsVisible}"/>
            </StackLayout>
            <controls:FadeInImage Source="hiphop_transparent" IsVisible="{Binding IsExtendedViewsVisible}">
                <OnPlatform x:TypeArguments="x:Int32">
                    <OnPlatform.Android>500</OnPlatform.Android>
                    <OnPlatform.iOS>0</OnPlatform.iOS>
                </OnPlatform>
            </controls:FadeInImage>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>