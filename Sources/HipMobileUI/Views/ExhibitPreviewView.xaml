<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PaderbornUniversity.SILab.Hip.Mobile.UI.Views.ExhibitPreviewView"
             Title="{Binding ExhibitName}"
             xmlns:container1="clr-namespace:PaderbornUniversity.SILab.Hip.Mobile.UI.Container;assembly=HipMobileUI"
             xmlns:map="clr-namespace:PaderbornUniversity.SILab.Hip.Mobile.UI.Map;assembly=HipMobileUI"
             xmlns:helpers="clr-namespace:PaderbornUniversity.SILab.Hip.Mobile.UI.Helpers;assembly=HipMobileUI">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Spacing="0" VerticalOptions="FillAndExpand">
            <Label Text="{Binding Question}" FontSize="Large" 
                HorizontalTextAlignment ="Start" Margin="10,0,0,0" VerticalOptions="Start"/>
            <BoxView  HeightRequest="2" Margin="0,0,0,0" Color="{StaticResource PrimaryDarkColor}"/>
            <Image Source="{Binding Image}" Aspect="AspectFill" BackgroundColor="White" VerticalOptions="Center"/>
            <BoxView  HeightRequest="2" Margin="0,0,0,0" Color="{StaticResource PrimaryDarkColor}"/>
            <StackLayout Orientation="Horizontal" Padding="16,8,8,8" 
                    BackgroundColor="#05000000" VerticalOptions="End">
                <Label Text="{helpers:Translate ExhibitOrRouteNearby_Confirm}" FontSize="12" TextColor="{StaticResource GreenDarkColor}"
                HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Confirm}" />
                        </Label.GestureRecognizers>
                    </Label>
                <BoxView WidthRequest="2" Color="{StaticResource GrayColor}"/>
                <Label Text="{helpers:Translate ExhibitOrRouteNearby_Reject}" FontSize="12" TextColor="{StaticResource GreenDarkColor}"
                HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Decline}" />
                    </Label.GestureRecognizers>
                </Label>
            </StackLayout>
            </StackLayout>

    </ContentPage.Content>
</ContentPage>